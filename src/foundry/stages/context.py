from __future__ import annotations

from ..config import Settings
from ..models import Task


def run(task: Task, settings: Settings) -> dict:
    """STUB: returns an empty context. Real implementation lands in Day 3+."""
    return {"files": []}
