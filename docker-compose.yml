services:
  foundry-agent:
    build: .
    container_name: foundry-agent
    volumes:
      - ./code:/app/code
      - ./agent/tasks:/app/agent/tasks
      - ./.env:/app/.env:ro
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true
